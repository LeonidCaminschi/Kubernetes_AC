apiVersion: v1
kind: Service
metadata:
  name: calculator-app
  namespace: calculator
spec:
  type: NodePort
  selector:
    app: calculator-app
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
